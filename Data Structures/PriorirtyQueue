#include <iostream>
#include <queue>

using namespace std;

int main()
{
	priority_queue<string> p;
	p.push("AA");
	p.push("BB");
	p.push("BBAA");
	p.push("BBAACC");
	p.push("bbaacc");
	printQueue(p);

	priority_queue<int> a;
	for (int i = 0; i < 6; i++)
	{
		a.push(i);
	}
	printQueue(a);
	
	cin.get();
	return 0;
}
